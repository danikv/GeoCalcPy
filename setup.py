from setuptools import setup, find_packages

setup(name='geocalc',
      version='0.1.0',
      url='https://github.com/danikv/GeoCalcPy',
      license='Mashov',
      author='',
      author_email='',
      description='',
      packages=find_packages(exclude=['tests']),
      long_description=open('README.md').read(),
      zip_safe=False)
